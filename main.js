$( document ).ready(function(){
    // jQuery Effects
    // 1. Element show, hide, toggle

    // 2. fadein fadeout fadetoggle

    // 3. slideUp slideDown slideToggle

   $('#data').click(function() {
       $('.content').slideToggle(1000);
   });
});
